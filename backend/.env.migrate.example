# Lokal → Render veri taşıma için
# Bu dosyayı .env.migrate olarak kopyalayın ve değerleri doldurun

# Lokal veritabanı (sizin .env'deki DATABASE_URL ile aynı)
SOURCE_DATABASE_URL="postgresql://user:password@host:port/database?sslmode=require"

# Render veritabanı (Render Dashboard → Environment'dan External URL)
# Dikkat: Tırnak içine alın, URL sonunda ?sslmode=require yeterli (tekrarlamayın)
TARGET_DATABASE_URL="postgresql://user:password@host:port/database?sslmode=require"
